# Identidad de marca

`logo-ltecobike.svg` está reservado para el archivo vectorial oficial. El componente `<x-storefront.logo>` usa ese SVG cuando existe y mantiene `logo-lteco.webp` como fallback. No sustituirlo por una vectorización automática o una versión generada por IA.
